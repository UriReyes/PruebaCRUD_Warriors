<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}CRUD PRUEBA{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('css/bootstrap.min.css') }}">
            <link rel="stylesheet" href="{{ asset('css/styles.css') }}">
        {% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
        {% block header %} 
            <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
                <div class="d-flex flex-wrap justify-content-center align-items-center">
                    <img src="{{ asset('icons/estudiante.png') }}" alt="student_icon" />
                     <a class="navbar-brand">Warriors Lab´s</a>
                </div>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="ml-5 collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav mr-auto">
                        <li class="nav-item active">
                            <a class="nav-link" href="{{ path('homepage') }}">Inicio<span class="sr-only">(current)</span></a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('get_estudiante') }}">Estudiantes</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('get_grupo') }}">Grupos</a>
                        </li>
                    </ul>
                </div>
            </nav>
        {% endblock %}

        {% block body %}{% endblock %}
        
        {% block javascripts %}{% endblock %}

        {% block footer %}
          <p class="text-center">Uriel Santiago Reyes Paredes &copy;</p>
        {% endblock %}
    </body>
</html>
